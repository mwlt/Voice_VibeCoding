# Dependencies
node_modules/

# Frontend build
dist/

# Rust / Tauri build output
src-tauri/target/
# Alternate cargo target dirs (repo root OR under src-tauri; diagnosis / TDD can be huge)
/target/
/target-*/
src-tauri/target-*/
**/target-test*/
src-tauri/preprocessed.xml

# Local third-party trees / extracted zips (not part of the app source)
_third_party/

# Logs & temp
*.log
*.tmp
.DS_Store
Thumbs.db
Desktop.ini

# Editor / IDE / local agent tooling
.vscode/
.idea/
.cursor/
.reasonix/
.codegraph/
reasonix.toml
.workbuddy/
*.code-workspace
vite.config.ts.timestamp-*.mjs

# Design source binaries (keep exported png/ico in docs/icon-3)
*.psb
*.psd

# Local logs & notes (not for repo)
logs/
语音效率及白屏优化.md
bug修复.md
# 本机双远端推送备忘（含个人仓库 URL）；禁止入库
LOCAL_GIT_PUSH.md

# Env & secrets
.env
.env.*
!.env.example
*.pem
*.key
credentials.json

# Accidental installer copies / duplicate downloads in repo root or docs
/*.msi
/*-setup.exe
docs/install-winuhid(1).ps1

tools/silent-updater/target/

# WDK NuGet restore + local KMDF build intermediates (keep shipped .sys / scripts)
src-tauri/assets/t1_hid_filter/driver/packages/
src-tauri/assets/t1_hid_filter/driver/obj/
src-tauri/assets/t1_hid_filter/driver/out/
src-tauri/assets/t1_hid_filter/driver/nuget.exe
docs/icon-3/桌面图标.psb
