# Dependencies
node_modules/

# Frontend build
dist/

# Rust / Tauri build output
src-tauri/target/
# Alternate cargo target dirs used for diagnosis / TDD (can be huge)
src-tauri/target-*/
src-tauri/preprocessed.xml

# Local third-party trees / extracted zips (not part of the app source)
_third_party/

# Logs & temp
*.log
*.tmp
.DS_Store
Thumbs.db
Desktop.ini

# Editor / IDE / local agent tooling
.vscode/
.idea/
.cursor/
.reasonix/
.codegraph/
reasonix.toml
.workbuddy/
*.code-workspace
vite.config.ts.timestamp-*.mjs

# Local logs & notes (not for repo)
logs/
语音效率及白屏优化.md
bug修复.md

# Env & secrets
.env
.env.*
!.env.example
*.pem
*.key
credentials.json

# Accidental installer copies / duplicate downloads in repo root or docs
/*.msi
/*-setup.exe
docs/install-winuhid(1).ps1

tools/silent-updater/target/

