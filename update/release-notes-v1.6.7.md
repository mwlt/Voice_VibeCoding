v1.6.7：

1. 全局设置改实时保存并新增 hide_dev_menus；隐藏语言项
2. 小米页：页面美化，电量图标、音频波形、虚拟声卡 dBFS 标尺、映射键图标
3. 增益保存后立即生效（voice_gain 热更新）
4. 自启仅 Run 单入口；单实例忽略重复 --minimized；WebView 恢复尊重托盘
5. 「启动后最小化到托盘」统一控制冷启动/自启/再点图标是否进托盘（仅下次启动生效）
